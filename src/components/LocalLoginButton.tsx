import React from 'react';

// LocalLoginButton deprecated — use GoogleLoginButton instead. Keep a no-op
// component here to avoid accidental imports since the previous file existed.
export default function LocalLoginButton() {
  return null;
}
